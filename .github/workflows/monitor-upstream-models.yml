name: Monitor Upstream Models

on:
  schedule:
    # Run daily at 02:00 UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      since:
        description: 'Baseline date for new updates (e.g. today, 7d, 30d, or YYYY-MM-DD)'
        required: false
        default: 'today'
        type: string
      repo:
        description: 'Upstream repository'
        required: false
        default: 'k2-fsa/sherpa-onnx'
        type: string
      tags:
        description: 'Release tag'
        required: false
        default: 'asr-models'
        type: string
      create_issue:
        description: 'Update tracking issue'
        required: false
        default: true
        type: boolean

permissions:
  contents: read
  issues: write

jobs:
  check-models:
    name: Check Upstream Models
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Check upstream models
        id: check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          REPO="${{ inputs.repo || 'k2-fsa/sherpa-onnx' }}"
          TAGS="${{ inputs.tags || 'asr-models' }}"
          SINCE="${{ inputs.since || 'today' }}"
          CREATE_ISSUE_FLAG=""
          if [ "${{ inputs.create_issue != false }}" = "true" ]; then
            CREATE_ISSUE_FLAG="--create-issue"
          fi

          python scripts/check_upstream_models.py \
            --registry registry/models.json \
            --repo "$REPO" \
            --tags "$TAGS" \
            --since "$SINCE" \
            --token "$GITHUB_TOKEN" \
            --output-dir output_report \
            $CREATE_ISSUE_FLAG

      - name: Upload monitor artifacts
        uses: actions/upload-artifact@v4
        with:
          name: upstream-models-report
          path: output_report/
          retention-days: 30
